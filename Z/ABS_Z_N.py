# Двойникова Валерия Алексеевна, гр. 4381.

# -----------------------------------------------------------------------------
# Основная функция
# -----------------------------------------------------------------------------

def ABS_Z_N_f(num):
    """
    *** Абсолютное значение целого числа, результат натуральное число ***
    num — словарь, число (может быть отрицательным через дополнительный ключ)
    Возврат — натуральное число в формате {'a': list[int]}
    raise ValueError, если число некорректное
    """
    check_integer_number(num) # проверка целого числа
    return {'a': num['a'][:]}  # просто копируем список цифр (абсолютное значение)

# -----------------------------------------------------------------------------
# Вспомогательные функции
# -----------------------------------------------------------------------------

def check_integer_number(num):
    """
    Проверка целого числа
    num — словарь {'a': list[int]}
    raise ValueError, если не целое или список пустой/не цифры
    """
    if not isinstance(num, dict):
        raise ValueError("число должно быть словарём")
    a = num.get('a')
    if not isinstance(a, list) or len(a) == 0:
        raise ValueError("*a* должно быть непустым списком")
    if not all(isinstance(d, int) and 0 <= d <= 9 for d in a):
        raise ValueError("все элементы списка a должны быть цифрами (0–9)")